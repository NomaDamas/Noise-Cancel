name: noise_cancel_app
description: NoiseCancel Flutter client
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2
  provider: ^6.1.2
  flutter_card_swiper: ^7.1.0
  url_launcher: ^6.3.2
  flutter_secure_storage: ^9.2.4
  google_fonts: ^6.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
